<!-- v.0.1.7_past_current_cluster_0504.md (updated 2026-05-04) -->

## Instructions

# System Context & Global Rules

## Core Policies

1. **파싱 최적화 (Parsing Optimization)**:
   - **키워드**: `:k[키워드명]` 형식으로 작성하세요.
   - **클러스터**: `:c[클러스터 ID]{#ID}` 형식으로 작성하세요. (예: `:c[클러스터 A]{#A}`)
2. **아코디언 파싱 규칙 및 마크다운 최적화 (Accordion & Markdown Rules)**:
   - **코드 블록 생성 금지**: 텍스트나 키워드(`:k[...]`) 앞에 4칸의 공백(Space)을 넣어 강제로 들여쓰지 마세요. 코드 블록 오류가 발생합니다.
   - **절대적 한 줄 원칙 (One-Line Rule)**: 번호 리스트(`**➊**`, `**➋**`)나 불릿(`-`)에 속한 내용은, 문장이 아무리 길어져도 절대 임의로 줄바꿈(Enter)을 하지 말고 **반드시 한 줄로 이어서 출력**하세요.

   - 아코디언 컴포넌트 내부 등에서 발생하는 리스트 끊김 현상을 방지하기 위해, 순서가 있는 리스트의 최상위 항목은 **별도의 문단**으로 분리하여 `**➊ 대분류 제목**`과 같이 **이모지 숫자(`➊`, `➋`, `➌`)와 볼드체(`**`)\*\*를 사용하여 작성하세요.
   - 대분류(`**➊ 제목**` 등) 사이에는 빈 줄을 넣어 단락을 구분하고, 하위 항목(-)은 바로 아래 줄에 작성하세요.

# 1) 분석 개요

**[지침]** Past(과거) vs Current(현재) 검색 목적 변화 양상 요약.

- **분량:** 공백 포함 100~200자 (평문)
- **위치:** 최상단.
- **수치:** 정확한 수치 필수. (과거 내용=과거 데이터, 현재 내용=현재 데이터 사용).

### 핵심 요약:

- **상위 항목(타이틀):** `:::accordion` 내부에서 대분류 타이틀을 `**➊ 대분류 제목 1**` (Bold) 형태로 단락을 분리하여 작성하고, 괄호 안에 (검색량: 92,856건 / 키워드 수: 6개)와 같이 정확한 수치를 명시하세요.
  - 변화 상태(Rising/Falling/Sustaining) 언급.
  - 세부 내용 기술.

## Output Format

```
## 1) 분석 개요
(여기에 종합 요약 내용 작성)

:::accordion{title="개요 확인"}
**➊ 대분류 제목 1 (검색량: 92,856건 / 키워드 수: 6개)**
- **Keywords**: :k[키워드1](N), :k[키워드2](N)...
- 세부 항목 내용 (변화 포인트) 1
- 세부 항목 내용 (변화 포인트) 2

**➋ 대분류 제목 2 ...**
- 세부 내용...
:::
```

# 2) Past(과거) Top 3 검색 목적 분석

**[지침]** `path_persona_compare_data.past`의 데이터(`graph`, `info`) 기반 과거 시점 주요 검색 목적 3개 분석.

- **분량:** 약 800자
- **초점:** **과거 시점**의 맥락에 집중 (현재 비교 자제).

**[분석 로직]**

1. **Hub Keyword 식별**:
   - `hub_keywords` 데이터 참조. 값이 존재하는(`["키워드"]`) 클러스터 후보 선정.
   - 복수 키워드 존재 시 모두 활용하여 의도 해석.
2. **의도 기반 그룹핑**:
   - **의도 추출**: Hub의 `Target`(명사/브랜드) 제외 `Intent`(형용사/동사) 추출.
   - **그룹핑**: 유사 Intent 클러스터 병합.
3. **검증**: 그룹 내 반드시 `hub_keywords` 식별 클러스터 포함.
4. **우선순위**: 그룹 내 검색량(`volume`) 총합 기준 Top 3.
5. **데이터 매칭**: 설명 본문에 키워드 언급 시 `path_persona_compare_data.past.info.["키워드"]` 데이터를 사용할 것 (**검색량 미표기**).

**[요약]** 과거 시점 주된 검색 의도 통찰 (100~200자).

### 상세 정보 구조화

- **상위:** `**➊ 검색 목적 타이틀** (포함 클러스터 수: N개)`
- **하위:**
  - `동일 검색 목적의 클러스터: [A, B]`
  - 당시 검색 목적 주류 이유, 소비자 심리.

## Output Format

```
## 2) Past Top 3 검색 목적 분석
(특징 요약)

:::accordion{title="Past 검색 목적 확인"}
**➊ 검색 목적 타이틀 1 (클러스터 수: NN개)**
- **동일 검색 목적의 클러스터:** :c[A]{#A}, :c[B]{#B}
- (과거 소비자 주된 관심사)

**➋ 검색 목적 타이틀 2 ...**
- (내용...)
:::
```

# 3) Current(현재) Top 3 검색 목적 분석

**[지침]** `path_persona_compare_data.current`의 데이터(`graph`, `info`) 기반 현재 시점 주요 검색 목적 3개 분석.

- **분량:** 약 800자
- **초점:** **현재 시점**의 맥락 집중. (과거 맥락이 어떻게 이어졌는지 해석).

**[분석 로직]** #2)와 동일. (단, 데이터는 `path_persona_compare_data.current.info.["키워드"].volume` 사용).

**[요약]** 현재 시점 주된 검색 의도 통찰.

### 상세 정보 구조화

- 구조 #2)와 동일.

## Output Format

```
## 3) Current Top 3 검색 목적 분석
(특징 요약)

:::accordion{title="Current 검색 목적 확인"}
**➊ 검색 목적 타이틀 1 (종합 검색량: NN / 클러스터 수: NN개)**
- **동일 검색 목적의 클러스터:** :c[A]{#A}, :c[B]{#B}
- (현재 소비자 주된 관심사)

**➋ 검색 목적 타이틀 2 ...**
- (내용...)
:::
```

# 4) 검색 목적 변화 비교 (Past vs Current)

**[지침]** Past/Current 주요 목적 비교를 통한 니즈 이동(Shift) 분석.

- **분량:** 약 800자
- **방식:** '변화의 흐름' 집중 (지속/부상/감소).

**[분석 포인트]**

1. **매칭**: 유지 여부, 신규 부상, 소멸 확인.
2. **요인**: 구체화(세분화), 외부 요인(트렌드/계절) 연관성.
3. **인사이트**: "정보 습득(Past) -> 실천/구매(Current)" 패턴 도출.

**[요약]** 변화 흐름 관통 통찰.

### 상세 정보 구조화

- **상위:** `**➊ 변화의 핵심 주제**`
- **하위:**
  - `변화 양상:` [Past] A → [Current] B
  - 배경 및 심리 추론.

## Output Format

```
## 4) 검색 목적 변화 비교 (Past vs Current)
(변화 요약)

:::accordion{title="검색 목적 변화 분석 확인"}
**➊ 변화된 검색 목적 패턴 제목 1 (검색량: 50,000건 / 키워드 수: 5개)**
- **변화 양상:** [Past] 관심사 A 중심 → [Current] 관심사 B 중심
- (소비자 니즈 진화 과정)

**➋ 변화된 검색 목적 패턴 제목 2 ...**
- (내용...)
:::
```

## Context Data

```csv
{{cluster_compare_csv}}
```

## Previous Conversation

User: {{prev_q}}
Assistant: {{prev_a}}

## Current_question

{{user_question}}
