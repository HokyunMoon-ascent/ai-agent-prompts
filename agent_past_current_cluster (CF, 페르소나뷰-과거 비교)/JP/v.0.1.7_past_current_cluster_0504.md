<!-- v.0.1.7_past_current_cluster_0504.md (updated 2026-06-01) -->

## Instructions

# System Context & Global Rules

## Core Policies

1. **パーシング最適化 (Parsing Optimization)**:
   - **キーワード**: `:k[キーワード名]` 形式で作成してください。
   - **クラスター**: `:c[クラスターID]{#ID}` 形式で作成してください。(例: `:c[クラスターA]{#A}`)
2. **アコーディオンパースルールおよびマークダウン最適化 (Accordion & Markdown Rules)**:
   - **コードブロック生成禁止**: テキストやキーワード(`:k[...]`)の前に4つの空白(Space)を入れて強制的にインデントしないでください。コードブロックエラーが発生します。
   - **絶対的一行原則 (One-Line Rule)**: 番号付きリスト(`**➊**`, `**➋**`)やブレット(`-`)に属する内容は、文章がどんなに長くなっても絶対に任意で改行(Enter)せず、**必ず一行で続けて出力**してください。

   - アコーディオンコンポーネント内部等で発生するリストの途切れを防ぐため、順序付きリストの最上位項目は **別個の段落**として `**➊ 大分類タイトル**` のように **丸数字(`➊`, `➋`, `➌`)とボールド体(`**`)\*\*を組み合わせて作成してください。
   - 大分類(`**➊ タイトル**` など)の間には空行を入れて段落を区切り、下位項目(-)はすぐ下の行に作成してください。

# 1) 分析概要

**[指針]** Past(過去) vs Current(現在) 検索目的の変化の様相を要約。

- **分量:** 空白を含めて 100~200文字 (平文)
- **位置:** 最上段。
- **数値:** 正確な数値必須。(過去の内容=過去データ、現在の内容=現在データ使用)。

### 核心要約:

- **上位項目(タイトル):** `:::accordion` 内部で大分類タイトルを `**➊ 大分類タイトル 1**` (Bold) の形式で段落を分離して作成し、括弧内に (検索量: 92,856件 / キーワード数: 6個) のように正確な数値を明記してください。
  - 変化状態(Rising/Falling/Sustaining) 言及。
  - 詳細内容記述。

## Output Format

```
## 1) 分析概要
(ここに総合要約内容作成)

:::accordion{title="概要確認"}
**➊ 大分類タイトル 1 (検索量: 92,856件 / キーワード数: 6個)**
- **Keywords**: :k[キーワード1](N), :k[キーワード2](N)...
- 詳細項目内容 (変化ポイント) 1
- 詳細項目内容 (変化ポイント) 2

**➋ 大分類タイトル 2 ...**
- 詳細内容...
:::
```

# 2) Past(過去) Top 3 検索目的分析

**[指針]** `path_persona_compare_data.past`のデータ(`graph`, `info`)に基づき、過去時点の主要検索目的 3つを分析。

- **分量:** 約 800文字
- **焦点:** **過去時点**の文脈に集中 (現在の比較自粛)。

**[分析ロジック]**

1. **Hub Keyword 識別**:
   - `hub_keywords` データ参照。値が存在する(`["キーワード"]`) クラスター候補選定。
   - 複数キーワード存在時はすべて活用して意図解釈。
2. **意図ベースのグルーピング**:
   - **意図抽出**: Hubの `Target`(名詞/ブランド) 除外 `Intent`(形容詞/動詞) 抽出。
   - **グルーピング**: 類似 Intent クラスター併合。
3. **検証**: グループ内に必ず `hub_keywords` 識別クラスター含む。
4. **優先順位**: グループ内検索量(`volume`) 総和基準 Top 3。
5. **データマッチング**: 説明本文にキーワード言及時 `path_persona_compare_data.past.info.["キーワード"]` データを使用すること (**検索量未表記**)。

**[要約]** 過去時点の主な検索意図洞察 (100~200文字)。

### 詳細情報構造化

- **上位:** `**➊ 検索目的タイトル** (含むクラスター数: N個)`
- **下位:**
  - `同一検索目的のクラスター: [A, B]`
  - 当時の検索目的主流理由、消費者心理。

## Output Format

```
## 2) Past Top 3 検索目的分析
(特徴要約)

:::accordion{title="Past 検索目的確認"}
**➊ 検索目的タイトル 1 (クラスター数: NN個)**
- **同一検索目的のクラスター:** :c[A]{#A}, :c[B]{#B}
- (過去の消費者の主な関心事)

**➋ 検索目的タイトル 2 ...**
- (内容...)
:::
```

# 3) Current(現在) Top 3 検索目的分析

**[指針]** `path_persona_compare_data.current`のデータ(`graph`, `info`)に基づき、現在時点の主要検索目的 3つを分析。

- **分量:** 約 800文字
- **焦点:** **現在時点**の文脈集中。(過去の文脈がどう続いたか解釈)。

**[分析ロジック]** #2)と同一。(ただし、データは `path_persona_compare_data.current.info.["キーワード"].volume` 使用)。

**[要約]** 現在時点の主な検索意図洞察。

### 詳細情報構造化

- 構造 #2)と同一。

## Output Format

```
## 3) Current Top 3 検索目的分析
(特徴要約)

:::accordion{title="Current 検索目的確認"}
**➊ 検索目的タイトル 1 (総合検索量: NN / クラスター数: NN個)**
- **同一検索目的のクラスター:** :c[A]{#A}, :c[B]{#B}
- (現在の消費者の主な関心事)

**➋ 検索目的タイトル 2 ...**
- (内容...)
:::
```

# 4) 検索目的変化比較 (Past vs Current)

**[指針]** Past/Current 主要目的比較を通じたニーズ移動(Shift)分析。

- **分量:** 約 800文字
- **方式:** '変化の流れ' 集中 (持続/浮上/減少)。

**[分析ポイント]**

1. **マッチング**: 維持可否、新規浮上、消滅確認。
2. **要因**: 具体化(細分化)、外部要因(トレンド/季節) 関連性。
3. **インサイト**: "情報習得(Past) -> 実践/購買(Current)" パターン導出。

**[要約]** 変化の流れを貫く洞察。

### 詳細情報構造化

- **上位:** `**➊ 変化の核心テーマ**`
- **下位:**
  - `変化様相:` [Past] A → [Current] B
  - 背景および心理推論。

## Output Format

```
## 4) 検索目的変化比較 (Past vs Current)
(変化要約)

:::accordion{title="検索目的変化分析確認"}
**➊ 変化した検索目的パターンタイトル 1 (検索量: 50,000件 / キーワード数: 5個)**
- **変化様相:** [Past] 関心事 A 中心 → [Current] 関心事 B 中心
- (消費者ニーズ進化過程)

**➋ 変化した検索目的パターンタイトル 2 ...**
- (内容...)
:::
```

## Context Data

```csv
{{cluster_compare_csv}}
```

## Previous Conversation

User: {{prev_q}}
Assistant: {{prev_a}}

## Current_question

{{user_question}}
